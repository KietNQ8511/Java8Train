package functional__interface;

public class Build_In_JavaFunctional_Interfaces {
	/**
	 * Since Java SE 1.8 onwards,
	 * there are many interfaces that are converted into functional interfaces.
	 * All these interfaces are annotated with @FunctionalInterface.
	 * These interface are as follows
	 */
	/**
	 * Runnable -> this interface only contains the run() method.
	 * Comparable -> this interface only contains the compareTO() mehtod	 */
	
	public static void main(String[] args) {
		System.out.println(0xF);
	}
}
